1)
Sign into Git Hub,create a repository and add files into it simply using the add button.

2)
$ git clone "paste the url from the repository here"
add your user email and user name
$git add
$git commit
$git push

3)
$git status

4)
$git rm -rf --cached$files

5)
$git reflog
choose ID of the file from which we should remove
$git reset --hard ID


6)
use git mergetool
and make necessary changes
$git rebase --continue


7)
git commit -m 'message'<filepath>